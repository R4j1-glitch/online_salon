# Leave VITE_API_URL empty (or set to /api) so Vite proxies requests to the
# backend and the PHP session cookie is sent same-origin.
#
# Only set VITE_API_URL to a full URL (e.g. http://localhost:8000/api) if
# your frontend is served from the same origin as the backend.
VITE_API_URL=/api
